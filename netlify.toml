[build]
  command = "pip install -r requirements.txt"
  functions = "netlify/functions"
  publish = "."

[dev]
  framework = "#custom"
  command = "python app.py"
  targetPort = 5000
  port = 8888
  publish = "."
  autoLaunch = true
